import { describe, expect, it } from 'vitest';

// Placeholder so `npm test` is green before any feature lands.
// Integration tests mirroring src/modules/ replace this.
describe('harness', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
